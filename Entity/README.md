# API实体说明
## 特殊类型
### 枚举类型
服务端对枚举类型的序列化（Json）结果为整形数字（32位有符号），因此客户端可以直接按整形类型进行反序列化。如果所用语言也支持枚举的，一般也可以直接反序列化为对应的枚举类型，但要注意枚举值对应。
### 泛型类型
在API参数和返回值中，服务端多用泛型类型，而非动态类型。客户端可以选择用泛型、所有类的父类（如Java的java.lang.Object类）、动态类型等接收、或者为特定API提供特定类型等方式均可，以能够反序列化为准。