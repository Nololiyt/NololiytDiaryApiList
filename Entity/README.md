# 实体相关总则
## 特殊类型说明
### 枚举类型
C#的枚举类型的本质为32位整形数字。服务端对其进行的序列化（Json）结果也为整形数字，因此客户端可以直接按整形类型反序列化。如果所用语言也支持枚举的，一般也可以反序列化为对应的枚举类型。
### 泛型类型
在API参数和返回值中，服务端多用泛型类型，而非动态类型。客户端可以选择用泛型、所有类的父类（如Java的java.lang.Object类）或者使用动态类型等接收、或者为特定API提供特定类型均可。